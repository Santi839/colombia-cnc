<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /Colombia_CNC/public/

  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [QSA,L]
</IfModule>

<FilesMatch "\.(json|lock|yml|yaml|log)$">
  Require all denied
</FilesMatch>
